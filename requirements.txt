numpy>=1.22.0  # Avoids older numpy versions requiring distutils
setuptools>=58.0.0  # Ensures build tools are available
wheel>=0.36.0  # Ensures correct wheel packaging
streamlit==1.14.0  # Or the latest compatible version
pandas==1.3.3  # Ensure compatibility with your app
openai==0.27.0  # Ensure compatibility with the OpenAI API
